# Synthetic trusted adapter

This directory belongs to the protected base checkout. The example generator reads the
candidate component as bounded data and returns fixed synthetic evidence stories. It never
imports or executes candidate code and is not a real company evaluator.
